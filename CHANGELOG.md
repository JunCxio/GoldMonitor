# 更新记录

## 1.0.19

- 今日概览新增全部、警报、通知异常、规则和行情筛选，可直接处理单条警报、重新提交异常通知、重新获取行情或编辑异常规则。
- 新增当前筛选范围内的警报批量处理和异常通知批量重新提交，并展示成功、失败、部分成功明细和最近操作记录。
- 完善组合待办提示和失败原因展示，帮助快速定位仍需处理的警报、通知、规则及行情问题。
- 修复多条待办时卡片被压缩、文字与按钮堆叠以及内容越过弹窗边界的问题；桌面端改为列表独立滚动，窄屏下保持工作区完整可用。
- 保持现有预警、通知、行情和今日概览数据格式兼容，升级后无需迁移本地数据。

## 1.0.18

- 新增今日概览工作台，在标题栏集中显示待处理数量，并汇总未处理警报、通知异常、规则异常和行情质量问题。
- 新增本机今日活动时间线，统一展示警报、持仓流水、风险分析和复盘笔记，并可跳转到对应功能。
- 新增人民币与美元持仓摘要；未处理事项可跨日保留，关闭概览时记录查看状态。
- 今日概览查看状态纳入诊断、完整数据归档与恢复流程，不修改现有预警、持仓和复盘数据格式。

## 1.0.17

- 修复应用重新启动后，已启用预警规则在首轮行情到达后仍停留在“等待数据”，必须编辑保存或切换启停才能恢复“监控中”的问题。
- 在美元或人民币价格首次可用时主动推送最新规则运行视图，并保持后续常规行情刷新不重复重绘预警页面。
- 保持现有预警规则、本地数据和通知逻辑兼容，升级后无需修改或重新保存规则。

## 1.0.16

- 使用稳定的 Windows 显示器设备标识保存任务栏金价目标，显示器重排、重连或 Explorer 重建后仍可匹配原显示器。
- 设置页支持自动选择、跟随主显示器和固定具体显示器，并展示显示器编号、分辨率及主副任务栏角色。
- 兼容旧版副任务栏序号设置，读取时自动映射到实际显示器并在下次保存时完成迁移。
- 固定显示器不可用时安全隐藏任务栏金价，不再跳转到其他屏幕；诊断信息同步记录实际显示器和分辨率。
- 保持现有本地数据、Socket 事件和其他桌面价格模式兼容，升级后无需手动迁移配置。

## 1.0.15

- 增强 Windows 任务栏金价窗口生命周期，监听 Explorer 任务栏重建通知并校验任务栏所有者，Explorer 或应用重启后可自动恢复显示。
- 新增多显示器任务栏支持，保持单实例并优先使用主任务栏；主屏不可用或空间不足时自动选择可用副屏任务栏。
- 使用 Windows UI Automation 获取任务栏按钮和通知区域图标的真实边界，固定应用或通知图标变化时自动调整位置。
- 收紧任务栏安全布局策略，空间不足时隐藏完整价格窗口，不再压缩进入可能覆盖任务按钮或通知区域的位置。
- 扩展设置页和诊断状态，记录任务栏类型、显示器索引、布局来源、恢复次数及候选失败原因；现有本地数据和设置无需迁移。

## 1.0.14

- 优化 Windows 任务栏金价宽度，根据当前价格和涨跌内容自适应收紧显示区域，减少任务栏空间占用。
- 将任务栏价格窗口改为 Windows 任务栏的受属窗口，移除全局置顶和交互后重复恢复层级的逻辑，修复左右键操作后消失或闪烁的问题。
- 扩展透明任务栏窗口的完整点击区域，价格、涨跌幅及文字间隔均可响应右键菜单，并修复价格文字覆盖右键菜单的问题。
- 保持现有桌面价格设置、本地数据和 Socket 事件契约兼容，macOS 菜单栏及 Windows 悬浮条行为不变。

## 1.0.13

- 优化 Windows 任务栏金价视觉，分别展示品牌、价格、涨跌方向和涨跌幅，并根据系统明暗主题、行情来源及涨跌状态调整颜色。
- 将任务栏窗口从色键透明改为逐像素 Alpha 合成，消除透明文字边缘的暗色毛边，并适配 100% 至 200% 的 Windows DPI 缩放。
- 优化任务栏窗口显示链路，布局未变化时不再重复定位、显示和重绘，修复点击任务栏价格后出现闪现的问题。
- 保持现有桌面价格设置、本地数据和 Socket 事件契约兼容，macOS 菜单栏及 Windows 悬浮条行为不变。

## 1.0.12

- 优化桌面悬浮窗交互，支持按需始终置顶、其他应用全屏时自动隐藏、锁定位置和重置位置，并扩展刷新行情、风险分析及显示行为切换等右键操作。
- 新增 Windows 任务栏金价模式，支持仅悬浮条、仅任务栏价格或两处同时显示；任务栏空间不足、自动隐藏或其他应用全屏时会自动避让或隐藏。
- 设置页新增桌面价格显示位置和运行状态提示，诊断报告同步记录 Windows 显示模式、任务栏窗口状态及悬浮窗行为。
- 保持默认桌面悬浮条行为和现有本地数据、设置及 Socket 事件契约兼容，macOS 菜单栏金价行为不变。

## 1.0.11

- 修复应用架构拆分后 Flask 自动静态路由与显式静态路由冲突，导致安装版 CSS、JavaScript 和图标资源返回 404 的问题。
- 新增真实 HTTP 静态资源回归测试，逐一验证首页渲染引用的资源可访问，并确保应用只注册一条静态资源路由。
- 保持现有本地数据文件、设置和 Socket 事件契约不变，升级后无需迁移用户数据。

## 1.0.10

- 完善设置页布局、分区和交互体验，调整导航结构并提升不同窗口尺寸下的可用性。
- 新增持仓规则历史模拟，可在保存前基于本地历史行情评估命中次数、冷却抑制和触发分布。
- 将根目录 `app.py` 收敛为兼容入口，拆分应用状态、启动、Socket、桌面、行情、预警、持仓和运维等运行时模块。
- 拆分 `static/app.js`，将设置、预警、持仓、风险分析、历史复盘和运维等前端职责迁移到独立模块。
- 修复 Windows 路径分隔符导致的跨平台测试失败，并移除凭据键名、错误详情等敏感日志上下文。
- 保持现有本地数据文件格式和 Socket 事件契约兼容，已有配置及历史数据无需迁移。

## 1.0.9

- 新增预警规则运行诊断，在规则详情中显示当前值、目标值、距触发差距、窗口样本和未参与评估的具体原因。
- 新增规则搜索、状态筛选、当前结果全选及批量启用、停用、重置和删除；批量操作先校验完整选择，再一次写入规则文件，避免部分成功。
- 新增单规则最近 30 天触发复盘，分别展示触发次数、通知送达率、已处理率和 24 小时后行情延续率，并显示实际通知渠道与冷却策略。
- 新增预警规则历史模拟，可在保存前使用本地 7 天、30 天或 90 天历史行情估算规则命中、冷却后触发、抑制次数和时段分布；持仓规则因缺少历史估值快照暂不模拟。
- 预警规则详情可直接筛选对应警报记录；警报搜索同时支持规则编号、名称和类型。
- 保持 `alert_rules.json` schema v1，不增加规则类型，也不改变 1.0.8 数据的升级与回滚路径。

## 1.0.8

- 新增统一预警中心，将价格阈值、短时波动、目标价观察和持仓提醒收敛为同一套规则模型与编辑界面，支持类型筛选、状态汇总、复制、启停、重置和删除。
- 新增规则级有效期、冷却时间和通知渠道配置；通知渠道可继承全局设置、指定本机/邮件/Webhook，或设为仅记录不发送。
- `alert_rules.json` 成为预警规则唯一事实来源；首次启动会一次性迁移旧版 `thresholds.json`、`watch_targets.json` 和 `portfolio_alerts.json`，迁移后旧入口通过兼容视图继续工作。
- 统一行情刷新中的规则评估链路，所有预警均携带规则编号、类型、名称、作用范围、触发条件和通知策略快照，避免多条旧链路重复判断。
- 警报记录、事件时间轴、预警有效性统计、配置备份、完整数据归档和诊断报告均已接入统一规则；历史警报可定位原规则，规则已删除时可从快照复制为新规则。

## 1.0.7

- 新增价格历史分层留存与自动粒度选择：原始行情保留 24 小时，1 分钟保留 30 天，5 分钟保留 90 天，1 小时保留 2 年，日线长期保留，并增加 30 日、90 日图表和事件时间轴范围。
- 通知状态升级为发送中、已送达和失败，记录尝试次数、开始/完成时间并执行最多三次有限重试；配置错误不重试，警报与每日摘要均记录最终投递结果。
- 新增完整本地数据归档与恢复，覆盖设置、密钥、持仓、流水、预警、复盘、缓存、滚动指标及 SQLite 数据库；恢复前校验版本、文件清单、大小、SHA-256、JSON 和 SQLite 完整性，失败时自动回滚。
- 新增四步首次使用向导，支持新安装自动展示、暂不设置、完成状态持久化和从运维设置重新打开；旧版本设置自动迁移为已完成。
- 新增持仓总收益曲线，按历史行情重放买卖流水，展示市值、未实现收益、已实现收益、总收益、收益率和最大回撤；原有曲线明确更名为已实现收益曲线。
- 新增预警有效性统计，分别展示通知送达率、确认/处理率和 24 小时后行情延续率，并明确行情延续率不代表预测准确率或投资建议。
- 新增内置数据源管理，可启停、排序、单独探测和恢复默认顺序；每类至少保留一个启用源，不开放自定义请求 URL。
- 新增版本化数据源滚动指标，按源保留最近 50 次探测的成功率、缓存率、平均/中位延迟、连续失败和最近恢复时间，并纳入诊断与完整归档。
- 行情质量改为可解释的连续评分，综合实时取数、缓存回退、当前主源滚动可靠性、连续失败、数据新鲜度和跨源价差，前端展示逐项扣分依据和当前主源。

## 1.0.6

- 新增本地每日摘要调度，支持按本地时间通过邮件或 Webhook 汇总最近 24 小时的行情、预警、风险分析、新闻、数据质量和持仓。
- 支持摘要预览和手动测试发送；程序当天晚启动时会补发，手动测试不会占用当天的计划发送资格。
- 新增版本化摘要状态文件和诊断清单，避免无可用通知渠道时重复执行计划任务。
- 新增本地复盘笔记，可在事件时间轴新增、编辑、删除独立笔记或关联具体事件。
- 复盘笔记纳入事件时间轴、每日摘要、复盘报告和诊断存储清单，并限制本地记录数量和单条内容长度。
- 新增内部行情源适配器接口，统一 4 个金价源和 3 个汇率源的元数据、请求结果与健康状态目录，同时保持现有固定优先级和失败回退顺序。
- 行情源适配器暂不开放用户排序或禁用配置，原有行情获取函数继续作为兼容入口。
- 新增本地风险分析历史对比，可选择两条记录并排查看；选择不持久化，且不会新增模型调用。
- 配置备份升级为 schema v1，完全排除敏感字段及其状态衍生字段，并在导入预检中显示备份格式和来源版本。
- 继续兼容无版本字段的旧版 v0 备份；旧版备份会在确认导入时迁移，未来版本或非法版本会在预检阶段被拒绝。

## 1.0.5

- 新增预警策略模板，支持保存、应用、重命名和删除常用预警配置。
- 增强配置备份、配置导入和诊断清单，纳入预警策略模板并保留导入预览与回滚能力。
- 限定模板保存范围，仅包含价格阈值、波动提醒和提醒行为配置，不包含 SMTP、Webhook URL 或密钥。

## 1.0.4

- 增加最近操作记录，在运维面板集中展示导出配置、生成诊断和打开目录的执行结果。
- 增强导出目录诊断闭环，打开目录失败时展示失败原因和恢复操作。
- 优化导出与诊断记录展示，保留路径信息并减少重复文案。

## 1.0.3

- 新增更新状态与诊断闭环，在设置与诊断摘要中展示当前版本、最新版本、检查状态、下载与安装状态。
- 增强复盘时间轴入口，支持从告警记录和风险分析历史直达对应时间窗口。
- 增强复盘报告内容质量，补充复盘结论、告警后走势和数据质量结论。
- 优化复盘报告数据状态口径，避免将冷启动等待行情误判为数据异常。

## 1.0.2

- 优化风险分析模型测试反馈，便于区分配置、网络和模型服务异常。
- 新增悬浮条置顶设置，允许用户按使用场景控制悬浮条是否保持在最上层。
- 增强风险分析失败诊断，展示失败原因、缺失数据和恢复建议。
- 新增诊断摘要一键复制，便于快速提供版本、行情状态、风险分析、悬浮条和日志概况。

## 1.0.1

- 增强持仓详情复盘视图，集中展示概览、流水、预警、复盘线索和关联流水。
- 优化风险分析入口，无有效行情数据时只保留重新获取路径，并在后端拒绝无行情分析请求。
- 增强风险分析数据依据展示，记录当前金价、行情源、缓存状态、样本规模和数据质量摘要。
- 新增风险分析历史的数据依据摘要，便于后续复盘判断分析可信度。

## 1.0.0

- 发布首个正式版本，提供实时黄金行情、人民币/美元价格切换、价格历史和短周期波动观察。
- 支持上涨、下跌和波动预警，提供邮件与 Webhook 通知、通知重发和本地警报记录。
- 提供持仓看板、流水管理、收益复盘、目标价观察和持仓提醒。
- 支持手动风险分析、行情质量检查、数据源状态展示和复盘时间线。
- 提供桌面窗口、系统托盘、菜单栏/悬浮价格、配置导入导出、诊断报告和自动更新能力。
