# 更新记录

## 1.3.1

- 新增 macOS 本地启动程序，支持双击 `GoldMonitor.command` 或执行 `scripts/start_mac.sh` 启动浏览器模式。
- 调整跨平台启动逻辑：非 Windows 默认进入 Web 模式，保留 Windows 桌面模式与托盘能力。

## 1.3.0

- 新增本地价格历史 SQLite 存储，并保留 JSON 侧写用于兼容现有导出和诊断链路。
- 新增图表事件标记，预警和风险分析会在价格走势中显示发生位置。
- 新增 Webhook 通知渠道，支持按预警级别控制发送，并提供测试入口。

## 1.2.2

- 合并右侧预警设置为规则列表，支持列表内展开编辑，并将邮件开关并入规则项。
- 修复右侧内容在窗口高度不足时无法完整访问的问题，右侧工作区支持纵向滚动。
- 增强系统托盘菜单，新增刷新行情、风险分析和切换桌面悬浮条入口。
- 优化安装器重装体验，安装前会关闭仍在运行的旧版程序，避免用户手动退出。
- 初始化 CodeGraph 索引配置，并将 `.codegraph/` 加入 Git 忽略规则。

## 1.2.1

- 优化整体界面样式，统一按钮尺寸、圆角、层级、悬停、按下、焦点和禁用状态。
- 改进窄窗口布局，主内容区在宽度缩小时支持内部滚动，避免按钮被遮挡。
- 优化设置弹窗在小窗口下的页签、滚动区域和底部操作按钮显示。

## 1.2.0

- 完善更新体验：更新检查结果显示更新时间和更新源，安装包下载阶段显示进度，静默更新完成后也会自动打开程序。
- 增强风险分析助手：新增快速、标准、深度分析模式，支持复制报告和导出 Markdown，继续保持手动触发和模型调用缓存。
- 增强桌面金价悬浮条：新增透明度、显示内容和贴边吸附设置，保留涨跌颜色、更新时间、数据源状态和右键菜单。
- 新增数据源健康面板：展示行情源、汇率源和缓存回退的状态、失败原因与响应耗时。
- 升级提醒系统：新增提醒冷却时间、静默时段、邮件标题和正文模板，并支持手动测试提醒。
- 新增历史记录与复盘：本地持久化价格历史，提供历史复盘弹窗和 CSV 导出。
- 新增安装与运维能力：支持配置导出、配置导入、恢复默认设置和诊断报告导出。

## 1.1.2

- 将默认更新源固定为 GitHub Release 的 `version.json`，旧配置为空时自动回落到默认更新源。
- 增加自动检查更新逻辑，配置了更新源后定时判断是否有新版本，并在更新按钮上显示状态。
- Release 发布说明改为从 `CHANGELOG.md` 读取，确保每次发布都包含明确变更内容。
- 桌面金价悬浮条改为双击打开主界面，单击只用于拖动或选中，不再误打开程序。
- 完善 README，补充功能说明、更新源、开发检查、打包和发布流程。

## 1.1.1

- 修复 GitHub Release 说明和 `version.json` 的编码问题。
- 使用无 BOM UTF-8 输出发布附件，避免页面或下载内容出现乱码。

## 1.1.0

- 新增桌面金价悬浮条增强：涨跌颜色、更新时间、数据源状态和右键菜单。
- 新增 GitHub Actions 发布流程，自动构建安装包并生成更新清单。
- 将 GitHub Release 作为安装包下载入口和程序更新源。
